import { OrderResultsPipe } from './order-results.pipe';

describe('OrderResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
